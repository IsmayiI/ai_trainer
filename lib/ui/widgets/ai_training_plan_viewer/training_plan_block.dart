import 'package:flutter/material.dart';

class TrainingPlanBlock extends StatelessWidget {
  const TrainingPlanBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
